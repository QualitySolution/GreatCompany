# QS: Великая компания

Стартовый Avalonia-проект приложения.

## Сборка

```bash
git submodule update --init --recursive
dotnet build GreatCompany.sln
```

Основной проект: `GreatCompany/GreatCompany.csproj`.

Библиотеки QS подключены сабмодулем `QSProjects` с ветки `develop`.
